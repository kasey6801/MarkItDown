# MarkItDown Local Frontend - pinned dependencies (single source of truth).
# Python: minimum 3.10; developed locally on 3.14 (macOS); Windows CI builds on 3.12.
#
# --- Runtime (required for `python app.py`) ---
# markitdown 0.0.2 declares every converter dependency as mandatory; the [all]
# extra is a no-op on this version (pip prints a harmless "does not provide the
# extra" warning) but is kept so this line stays correct if the pin is ever
# bumped to >=0.1.0, where converters moved into extras.
markitdown[all]==0.0.2
Flask==3.1.3
# Not pulled in by markitdown 0.0.2, but referenced by both PyInstaller specs
# (hiddenimports: lxml.html/lxml.etree and PIL.* image plugins) and used by the
# bundled app for HTML and image conversion:
lxml==6.0.2
Pillow==12.1.1
#
# --- Build tooling (packaging only; not needed to run the app from source) ---
pyinstaller==6.19.0
